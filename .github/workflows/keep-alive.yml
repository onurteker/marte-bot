name: Keep Marte Bot Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Marte Bot
        run: |
          curl -s --max-time 60 https://marte-bot-o1oa.onrender.com/ || true
